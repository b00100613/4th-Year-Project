Install Python3

Clone the repo

cd <repo location>

pip install virtualenv

virtualenv venv

source venv/bin/activate

pip install Flask

pip install -r requirements.txt

python3 app.py

The app should start
